type Query {
  foods: [Food!]!
  days: [Day]
  food(name: String, id: Int): Food
  day(date: Date, id: Int): Day
}
type Mutation {
  createFood(name: String!, color: String): Food!
  createDay(date: Date!): Day!
  updateFoodDate(id: Int!, date: Date!): Food
}
